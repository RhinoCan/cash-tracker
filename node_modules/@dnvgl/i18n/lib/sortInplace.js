import { sortByInplace } from "./sortByInplace";
export function sortInplace(source, order, locale) {
    var undefinedValuesCounter = 0;
    for (var i = 0; i < source.length; i++) {
        var element = source[i];
        if (element === undefined) {
            source.splice(i, 1);
            undefinedValuesCounter++;
            i--;
        }
    }
    sortByInplace(source, function (x) { return x; }, order, locale);
    if (undefinedValuesCounter) {
        if (order === "desc" || order === "descend") {
            for (var i = 0; i < undefinedValuesCounter; ++i) {
                source.push(undefined);
            }
        }
        else {
            for (var i = 0; i < undefinedValuesCounter; ++i) {
                source.unshift(undefined);
            }
        }
    }
    return source;
}
