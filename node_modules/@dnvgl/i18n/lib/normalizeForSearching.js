export function normalizeForSearching(value, locale) {
    return value
        .trim()
        .replace(/\s\s+/g, ' ')
        .toLocaleLowerCase(locale)
        .normalize("NFD")
        .replace(/[\u0300-\u036f]/g, "");
}
