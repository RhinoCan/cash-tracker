import { iso4217Currencies } from "./internal/iso4217Currencies";
export function findIso4217Currency(code) {
    var _a;
    if (code === 532) {
        var xcgCurrency = iso4217Currencies.find(function (x) { return x.alpha3Code === "XCG"; });
        var now = new Date();
        if (now >= new Date(xcgCurrency.introducedIn)) {
            (_a = console === null || console === void 0 ? void 0 : console.warn) === null || _a === void 0 ? void 0 : _a.call(console, "There are two currencies with the same currency numerical code (XCG, ANG)."
                + " The new XCG currency is returned which ultimately replaces ANG."
                + " To make sure you select the intended currency please use alpha 3 code.");
            return xcgCurrency;
        }
        return iso4217Currencies.find(function (x) { return x.alpha3Code === "ANG"; });
    }
    var predicate = typeof code === "string"
        ? function (x) { return x.alpha3Code === code; }
        : function (x) { return x.numericCode === code; };
    return iso4217Currencies.find(predicate);
}
