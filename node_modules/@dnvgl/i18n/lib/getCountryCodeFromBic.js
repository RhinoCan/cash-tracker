import { iso3166Countries } from "./internal/iso3166Countries";
import { isValidBicStructure } from "./internal/isValidBicStructure";
export function getCountryCodeFromBic(bic) {
    if (!isValidBicStructure(bic)) {
        return undefined;
    }
    var code = bic.substring(4, 6);
    if (code === "XK") {
        return "XK";
    }
    if (iso3166Countries.some(function (x) { return x.alpha2Code === code; })) {
        return code;
    }
    return undefined;
}
