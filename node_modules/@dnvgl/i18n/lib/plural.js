import { isInRange } from "./internal/isInRange";
function germanAlike(n) {
    return n === 1 ? 0 : 1;
}
;
function chinese() {
    return 0;
}
;
function polish(n) {
    if (n === 1)
        return 0;
    return isInRange(n % 10, 2, 4, true, true) && !isInRange(n % 100, 10, 20, true, false)
        ? 1
        : 2;
}
;
function resolvePluralization(lang) {
    switch (lang) {
        case "en":
        case "de":
        case "no":
        case "es":
            return germanAlike;
        case "pl":
            return polish;
        case "zh":
            return chinese;
        default:
            return germanAlike;
    }
}
export function plural(wordPattern, count, lang, separator) {
    var _a;
    if (separator === void 0) { separator = "|"; }
    var wordIndex = resolvePluralization(lang)(count), words = Array.isArray(wordPattern) ? wordPattern : wordPattern.split(separator);
    return (_a = words[(words.length - 1 < wordIndex) ? 0 : wordIndex]) !== null && _a !== void 0 ? _a : "";
}
