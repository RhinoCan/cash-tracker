import { formatNumberToFixed } from "./formatNumberToFixed";
var defaultOptions = { precision: 2, useBankersRounding: true };
export function formatMoney(value, options, locale) {
    var _a;
    var formatOptions = typeof options === "number"
        ? {
            useBankersRounding: defaultOptions.useBankersRounding,
            precision: options
        }
        : options !== undefined
            ? {
                useBankersRounding: defaultOptions.useBankersRounding,
                precision: (_a = options.precision) !== null && _a !== void 0 ? _a : defaultOptions.precision,
                currency: options.currency,
                currencyDisplay: options.currencyDisplay,
                currencySign: options.currencySign
            }
            : defaultOptions;
    return formatNumberToFixed(value, formatOptions, locale);
}
