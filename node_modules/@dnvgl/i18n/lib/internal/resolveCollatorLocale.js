import { createCollator } from "./createCollator";
export function resolveCollatorLocale(source) {
    return !source
        ? createCollator()
        : source instanceof Intl.Collator
            ? source
            : createCollator(source);
}
