var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var yearFormat = { year: 'numeric' }, monthFormat = __assign(__assign({}, yearFormat), { month: 'numeric' }), dayFormat = __assign(__assign({}, monthFormat), { day: 'numeric' }), minuteFormat = __assign(__assign({}, dayFormat), { hour: 'numeric', minute: 'numeric' }), secondFormat = __assign(__assign({}, minuteFormat), { second: 'numeric' });
export function resolveDateTimeFormatOptions(precision) {
    switch (precision) {
        case "years":
        case "y":
            return yearFormat;
        case "months":
        case "M":
            return monthFormat;
        case "days":
        case "d":
            return dayFormat;
        case "minutes":
        case "m":
            return minuteFormat;
        case "seconds":
        case "s":
            return secondFormat;
    }
}
