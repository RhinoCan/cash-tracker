import { iso3166Countries } from "./internal/iso3166Countries";
export function findIso3166Country(code) {
    var predicate = typeof code === "string"
        ? code.length === 2
            ? function (x) { return x.alpha2Code === code; }
            : function (x) { return x.alpha3Code === code; }
        : function (x) { return x.numericCode === code; };
    return iso3166Countries.find(predicate);
}
