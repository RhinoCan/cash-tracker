import { convertToDate } from "./internal/convertToDate";
export function formatDateToISO(value, includeTime) {
    if (includeTime === void 0) { includeTime = false; }
    var isoString = convertToDate(value).toISOString();
    return includeTime ? isoString : isoString.substring(0, isoString.indexOf('T'));
}
