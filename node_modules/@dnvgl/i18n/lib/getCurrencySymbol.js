import { findIso4217Currency } from "./findIso4217Currency";
import { getSeparator } from "./internal/getSeparator";
export function getCurrencySymbol(options, locale) {
    var _a, _b;
    if (typeof options === "string") {
        return getCurrencySymbolString(options, undefined, locale);
    }
    if (typeof options === "number") {
        return getCurrencySymbolString((_a = findIso4217Currency(options)) === null || _a === void 0 ? void 0 : _a.alpha3Code, undefined, locale);
    }
    var code = typeof options.currency === "string"
        ? options.currency
        : (_b = findIso4217Currency(options.currency)) === null || _b === void 0 ? void 0 : _b.alpha3Code;
    return getCurrencySymbolString(code, options.currencyDisplay, locale);
}
function getCurrencySymbolString(currency, currencyDisplay, locale) {
    if (!currency) {
        return "";
    }
    var intlOptions = {
        style: "currency",
        currency: currency,
        currencyDisplay: currencyDisplay !== null && currencyDisplay !== void 0 ? currencyDisplay : "symbol"
    };
    return getSeparator(1, "currency", intlOptions, locale);
}
