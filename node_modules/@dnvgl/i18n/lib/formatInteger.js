import { formatNumberToFixed } from "./formatNumberToFixed";
var defaultOptions = { precision: 0 };
export function formatInteger(value, locale) {
    return formatNumberToFixed(value, defaultOptions, locale);
}
