import { iso3166Countries } from "./internal/iso3166Countries";
import { isValidIbanStructure } from "./internal/isValidIbanStructure";
import { removeWhitespaces } from "./internal/removeWhitespaces";
export function getCountryCodeFromIban(iban, options) {
    if ((options === null || options === void 0 ? void 0 : options.removeWhitespaces) === true) {
        iban = removeWhitespaces(iban);
    }
    if ((options === null || options === void 0 ? void 0 : options.validateStructure) !== false && !isValidIbanStructure(iban)) {
        return undefined;
    }
    var code = iban.substring(0, 2);
    if (code === "XK") {
        return "XK";
    }
    if (iso3166Countries.some(function (x) { return x.alpha2Code === code; })) {
        return code;
    }
    return undefined;
}
